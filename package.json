{
  "name": "cursor-agent-orchestrator",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "db:start": "npm run start --workspace=@orchestrator/local-db",
    "db:stop": "npm run stop --workspace=@orchestrator/local-db",
    "db:logs": "npm run logs --workspace=@orchestrator/local-db",
    "db:destroy": "npm run destroy --workspace=@orchestrator/local-db",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "npm run dev --workspace=@orchestrator/server",
    "dev:web": "npm run dev --workspace=@orchestrator/web",
    "build": "npm run build --workspaces",
    "build:server": "npm run build --workspace=@orchestrator/server",
    "build:web": "npm run build --workspace=@orchestrator/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
